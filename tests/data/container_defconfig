hello:
	echo Hello

.id:
	id

.home:
	test "$${HOME}"
	test -d "$${HOME}"
	touch "$${HOME}/write_test"
	rm -f "$${HOME}/write_test"
	echo "$${HOME}"

.volumes:
	test "$${TEST_FILE}"
	test -f "$${TEST_FILE}"
	test -f /opt/test_file
	pwd

.os_release:
	. /etc/os-release && echo $$NAME
