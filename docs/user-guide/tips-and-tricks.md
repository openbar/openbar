# Tips & Tricks

<!-- TODO: Collect useful patterns from the codebase and real-world usage.
  Candidates:
  - Switching container engine via .bashrc (OB_CONTAINER_ENGINE)
  - Sharing Yocto download/sstate caches across projects (DL_DIR, SSTATE_DIR via .bashrc)
  - Using OB_CONTAINER_RUN_EXTRA_ARGS to pass extra flags (e.g. --network=host)
  - Using the O= option to redirect build output
  - Using `make foreach` in CI to validate all defconfigs
  - SSH agent forwarding (works automatically, nothing to configure)
  - Debugging a broken container build: make shell
-->
